# Story 5.1: Order and LineItem Models

## Status
Done

## Story
**As a** developer,
**I want** `Order` and `LineItem` models that snapshot price/quantity at checkout time,
**so that** historical orders remain accurate even if product prices change later.

## Acceptance Criteria
1. `Order` has `shop_id`, `user_id` (buyer, nullable for guest checkout if supported), `status` (enum: `pending`, `paid`, `fulfilled`, `cancelled`), `total_cents`.
2. `LineItem` has `order_id`, `product_id`, `quantity`, `unit_price_cents` (copied from the `CartItem` at checkout time).
3. A model spec verifies that changing a `Product`'s `price_cents` after an order is placed does not alter the `LineItem`'s stored `unit_price_cents`.

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled — manual review only.

## Tasks / Subtasks
- [x] Task 1: `Order` model + migration, `acts_as_tenant(:shop)`, `status` enum (AC: 1)
- [x] Task 2: `LineItem` model + migration (AC: 2)
- [x] Task 3: Model spec for price immutability after the fact (AC: 3)

## Dev Notes
- Schema: `docs/architecture/schema-design.md` section "Order / LineItem".
- Depends on Epic 4 (`Cart`/`CartItem`) being implemented first, since `LineItem.unit_price_cents` is copied from the corresponding `CartItem`, not from `Product` directly.
- `Shop#has_many :orders, dependent: :destroy` needed (same pattern as `:products`).

### Testing
- `spec/models/order_spec.rb`, `spec/models/line_item_spec.rb`.

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-18 | 0.1 | Story criada a partir de `docs/prd/epic-5-checkout-orders.md`. | @sm (River) |
| 2026-06-18 | 0.2 | GO — Draft → Ready. | @po (Pax) |
| 2026-06-18 | 0.3 | Implementado e verificado; Ready → InProgress → InReview. | @dev (Dex) |
| 2026-06-18 | 0.4 | QA PASS — InReview → Done. | @qa (Quinn) |

## Dev Agent Record
### Agent Model Used
claude-sonnet-4-6

### Completion Notes List
- `Shop#has_many :orders, dependent: :destroy` adicionado, mesmo padrão de `:products`/`:carts`.
- `Order` usa `acts_as_tenant(:shop)` + enum `status` (`pending/paid/fulfilled/cancelled`); `LineItem` não é tenant-scoped diretamente (escopado via `order`).

### File List
- `db/migrate/..._create_orders.rb`, `db/migrate/..._create_line_items.rb` (novos)
- `app/models/order.rb`, `app/models/line_item.rb` (novos)
- `app/models/shop.rb` (modificado)
- `spec/models/order_spec.rb`, `spec/models/line_item_spec.rb`, `spec/factories/orders.rb`, `spec/factories/line_items.rb` (novos)

## QA Results
**Verdict:** PASS
- `bundle exec rspec spec/models/order_spec.rb spec/models/line_item_spec.rb` → 4 examples, 0 failures, incluindo a imutabilidade de `unit_price_cents` após alteração do preço do produto.
