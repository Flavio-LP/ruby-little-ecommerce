# Story 3.3: Public Storefront Product Listing

## Status
Done

## Story
**As a** customer,
**I want** to browse a shop's public product listing,
**so that** I can find items to buy before any cart functionality exists.

## Acceptance Criteria
1. `/:shop_slug/produtos` is a public (no auth required) route listing only `active: true` products for that shop.
2. Inactive products and products from other shops never appear, verified by a feature spec with multiple shops/products fixtures.
3. Each product links to a detail page (`/:shop_slug/produtos/:id`) showing full description and price.

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled — manual review only.

## Tasks / Subtasks
- [x] Task 1: `Public::ProductsController#index`/`#show` (AC: 1, 3)
- [x] Task 2: Feature spec covering multi-shop/active isolation (AC: 2)

## Dev Notes
- Depends on Story 3.1 (`Product` model). No authentication required — this controller does not call `authenticate_user!`.
- Tenant resolution from `ApplicationController` (Story 2.1) already scopes `Product.all` to the current shop via `acts_as_tenant`; this controller just adds `.where(active: true)` on top.
- Route: `scope ':shop_slug' do resources :produtos, controller: 'public/products', only: [:index, :show] end` (kept outside the `admin` namespace, per `docs/framework/source-tree.md`'s `app/controllers/public/`).

### Testing
- `spec/features/public_storefront_spec.rb`.

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-18 | 0.1 | Story criada a partir do Epic 3. | @sm (River) |
| 2026-06-18 | 0.2 | GO — Draft → Ready. | @po (Pax) |
| 2026-06-18 | 0.3 | Implementado e verificado; Ready → InProgress → InReview. | @dev (Dex) |
| 2026-06-18 | 0.4 | QA PASS — InReview → Done. | @qa (Quinn) |

## Dev Agent Record
### Agent Model Used
claude-sonnet-4-6

### Completion Notes List
- `Public::ProductsController` não chama `authenticate_user!` — rota pública de fato.
- `resources :produtos, controller: "public/products", only: %i[index show]` gera `produtos_path`/`produto_path` (Rails singulariza "produtos" → "produto" via a regra genérica de remoção de "s").

### File List
- `app/controllers/public/products_controller.rb` (novo)
- `app/views/public/products/index.html.erb`, `show.html.erb` (novos)
- `config/routes.rb` (modificado)
- `spec/features/public_storefront_spec.rb` (novo)

## QA Results
**Verdict:** PASS
- `bundle exec rspec spec/features/public_storefront_spec.rb` → 2 examples, 0 failures (isolamento multi-loja + produto inativo confirmado, página de detalhe confirmada).
- Verificado também via `curl` contra o app real: `/curl-test-shop/produtos` → 200.
