# Story 1.4: CanCanCan Authorization Skeleton

## Status
Done

## Story
**As a** developer,
**I want** CanCanCan installed with an `Ability` class that branches on `User#role`,
**so that** authorization logic has a single, centralized location from the start of the project.

## Acceptance Criteria
1. `rails generate cancan:ability` has been run, producing `app/models/ability.rb`.
2. `Ability` defines distinct rule blocks for `role == "seller"` and `role == "customer"` (placeholders pending later epics).
3. `ApplicationController` includes `CanCan::ControllerAdditions` and rescues `CanCan::AccessDenied` with a redirect + flash message.
4. A request spec verifies that an unauthorized action raises/handles `CanCan::AccessDenied` as expected.

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled — manual review only.

## Tasks / Subtasks
- [x] Task 1: Generate `Ability` class (AC: 1)
- [x] Task 2: Implement role-branched rule blocks (AC: 2)
- [x] Task 3: Wire `rescue_from CanCan::AccessDenied` in `ApplicationController` (AC: 3)
- [x] Task 4: Request spec for access denial, using a throwaway controller/route since no real protected resource exists yet (AC: 4)

## Dev Notes
- Depends on Story 1.3 (`User#role`).
- Critical rule (`docs/framework/coding-standards.md`): authorization always via `Ability`/`authorize!`, never inline role checks.
- This story's `Ability` rules are intentionally minimal placeholders — `shop_id`-scoped rules for `Product`/`Order` are added in Epic 2 Story 2.4 once those models exist.

### Testing
- `spec/requests/` for the access-denial behavior; `spec/models/ability_spec.rb` style unit test acceptable too.

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-17 | 0.1 | Story criada a partir do Epic 1. | @sm (River) |
| 2026-06-17 | 0.2 | GO — Draft → Ready. | @po (Pax) |
| 2026-06-18 | 0.3 | Implementado e verificado; Ready → InProgress → InReview. | @dev (Dex) |
| 2026-06-18 | 0.4 | QA PASS — InReview → Done. | @qa (Quinn) |

## Dev Agent Record
### Agent Model Used
claude-sonnet-4-6

### Completion Notes List
- `Ability` com blocos `if user.seller? / elsif user.customer?` vazios (comentados com referência a onde as regras reais entram — Epic 2 Story 2.4 para sellers, Epic 4/5 para customers).
- `rescue_from CanCan::AccessDenied` usa `redirect_back fallback_location: "/"` (string literal, não `root_path`, já que nenhuma rota raiz existe ainda).
- Request spec usa um controller/rota descartável definidos dentro do próprio spec (`Rails.application.routes.draw` em `before`, `reload_routes!` em `after`), já que nenhum recurso protegido real existe nesta Epic.

### File List
- `app/models/ability.rb` (novo)
- `app/controllers/application_controller.rb` (modificado)
- `spec/requests/access_denied_spec.rb` (novo)

## QA Results
**Verdict:** PASS
- `bundle exec rspec spec/requests/access_denied_spec.rb` → 1 example, 0 failures (302 + flash[:alert] presente).
- Suíte completa (`bundle exec rspec`) → 4 examples, 0 failures após o fix do `RAILS_ENV` (ver Story 1.3 QA Results).
