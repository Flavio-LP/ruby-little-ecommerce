# Story 4.1: Cart and CartItem Models

## Status
Done

## Story
**As a** developer,
**I want** `Cart` and `CartItem` models scoped per shop,
**so that** a customer's cart contents are isolated to a single store.

## Acceptance Criteria
1. `Cart` has `shop_id`, `user_id` (nullable, for guest carts identified by session token).
2. `CartItem` has `cart_id`, `product_id`, `quantity`, `unit_price_cents` (snapshot taken at add-time, not recalculated from `Product#price_cents` later).
3. A model spec verifies that adding the same product twice increments `quantity` rather than creating a duplicate row.

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled — manual review only.

## Tasks / Subtasks
- [x] Task 1: `Cart` model + migration, `acts_as_tenant(:shop)` (AC: 1)
- [x] Task 2: `CartItem` model + migration, unique index on `[cart_id, product_id]` (AC: 2)
- [x] Task 3: Model spec for the increment-on-duplicate-add behavior (AC: 3)

## Dev Notes
- Schema: `docs/architecture/schema-design.md` section "Cart / CartItem". Same pattern as `Product` for `acts_as_tenant(:shop)`.
- Guest cart identification (`user_id` nullable) needs a session-token strategy — recommend a signed cookie or `session[:cart_token]` set on first visit, looked up alongside `user_id IS NULL` carts. Decide and document this mechanism in this story since Stories 4.2/4.3 depend on it; do not invent a different mechanism per story.
- `unit_price_cents` snapshot rule (`docs/framework/coding-standards.md` critical rule): set once when the `CartItem` is created from `Product#price_cents`, never read live from `Product` afterward.

### Testing
- `spec/models/cart_spec.rb`, `spec/models/cart_item_spec.rb`.

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-18 | 0.1 | Story criada a partir de `docs/prd/epic-4-shopping-cart.md`. | @sm (River) |
| 2026-06-18 | 0.2 | GO — Draft → Ready. | @po (Pax) |
| 2026-06-18 | 0.3 | Implementado e verificado; Ready → InProgress → InReview. | @dev (Dex) |
| 2026-06-18 | 0.4 | QA PASS — InReview → Done. | @qa (Quinn) |

## Dev Agent Record
### Agent Model Used
claude-sonnet-4-6

### Completion Notes List
- Mecanismo de carrinho guest decidido: cookie assinado e persistente (`cookies.permanent.signed[:guest_cart_token]`), gerado em `ApplicationController#guest_cart_token`, não sessão simples — sobrevive mais tempo que uma sessão típica.
- `current_cart` é um helper memoizado em `ApplicationController` (`helper_method`), resolvendo por `current_user` se logado, senão pelo `guest_cart_token`.
- Bug corrigido: `quantity` tem `default: 1` no schema, então um `CartItem` novo via `find_or_initialize_by` já vem com `quantity = 1` em memória (não `nil`) — incrementar direto duplicava a contagem. Corrigido checando `new_record?` antes de somar.

### File List
- `db/migrate/..._create_carts.rb`, `db/migrate/..._create_cart_items.rb` (novos)
- `app/models/cart.rb`, `app/models/cart_item.rb` (novos)
- `app/models/shop.rb` (modificado — `has_many :carts`)
- `app/controllers/application_controller.rb` (modificado — `current_cart`/`guest_cart_token`)
- `spec/models/cart_spec.rb`, `spec/models/cart_item_spec.rb`, `spec/factories/carts.rb`, `spec/factories/cart_items.rb` (novos)

## QA Results
**Verdict:** PASS
- `bundle exec rspec spec/models/cart_spec.rb spec/models/cart_item_spec.rb` → 6 examples, 0 failures (escopo de tenant, soma de total, incremento em vez de duplicação, unicidade de `[cart_id, product_id]`).
