# Story 5.3: Seller Order Visibility

## Status
Done

## Story
**As a** seller,
**I want** to see orders placed in my shop,
**so that** I know what to fulfill.

## Acceptance Criteria
1. `/:shop_slug/admin/orders` lists orders for the current shop only (tenant-scoped via `acts_as_tenant`), ordered most-recent-first.
2. Each order row links to a detail view showing line items, quantities, and total.
3. A request spec verifies a seller cannot view another shop's orders by guessing an order ID (403/404).

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled — manual review only.

## Tasks / Subtasks
- [x] Task 1: `Admin::OrdersController#index`/`#show` (AC: 1, 2)
- [x] Task 2: `Ability` rule for `Order` (same `shop_id == user.shop_id` pattern as `Product`/`Shop`) (AC: 3)
- [x] Task 3: Request spec for cross-tenant denial (AC: 3)
- [x] Task 4: Dashboard "Orders" placeholder card (from Story 2.3) updated to link here

## Dev Notes
- Depends on Story 5.1/5.2 (`Order` model + at least one order created via checkout).
- Same dual-layer pattern as Story 3.2 (`Product`): `acts_as_tenant` default_scope already returns 404 for cross-tenant `Order.find`, `authorize!` is the second layer.
- Routes: `only: %i[index show]`, no create/update/destroy exposed here (orders are created only via checkout, never directly by sellers).

### Testing
- `spec/requests/admin/orders_spec.rb`.

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-18 | 0.1 | Story criada a partir do Epic 5. | @sm (River) |
| 2026-06-18 | 0.2 | GO — Draft → Ready. | @po (Pax) |
| 2026-06-18 | 0.3 | Implementado e verificado; Ready → InProgress → InReview. | @dev (Dex) |
| 2026-06-18 | 0.4 | QA PASS — InReview → Done. | @qa (Quinn) |

## Dev Agent Record
### Agent Model Used
claude-sonnet-4-6

### Completion Notes List
- `Ability` ganhou `can %i[read], Order, shop_id: user.shop_id` — somente leitura (sem `:manage`), já que pedidos só são criados via checkout, nunca diretamente por vendedores.
- Mesma defesa em profundidade do `Product` (Story 3.2): `Order.find` já é filtrado pelo `acts_as_tenant`, retornando 404 antes do `authorize!` rodar para IDs de outra loja.

### File List
- `app/controllers/admin/orders_controller.rb` (novo)
- `app/views/admin/orders/index.html.erb`, `show.html.erb` (novos)
- `app/views/admin/dashboard/show.html.erb` (modificado — link para pedidos)
- `app/models/ability.rb` (modificado)
- `config/routes.rb` (modificado)
- `spec/requests/admin/orders_spec.rb` (novo)

## QA Results
**Verdict:** PASS
- `bundle exec rspec spec/requests/admin/orders_spec.rb` → 2 examples, 0 failures (lista escopada por loja, 404 cross-tenant).
- Verificado também via app real: vendedor logado em `/curl-test-shop/admin/orders` vê os pedidos da própria loja (status `pending`, total `$15.00`) criados durante os testes manuais desta sessão.
