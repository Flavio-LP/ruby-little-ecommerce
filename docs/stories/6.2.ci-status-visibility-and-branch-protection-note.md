# Story 6.2: CI Status Visibility and Branch Protection Note

## Status
Ready

## Story
**As a** maintainer,
**I want** CI status clearly visible and documented as a required check,
**so that** the Brakeman gate cannot be silently bypassed.

## Acceptance Criteria
1. README (or `docs/`) documents that the `ci` workflow (lint, rspec, brakeman) is expected to be set as a required status check for the default branch (actual GitHub branch protection configuration is a `@devops`-owned, repo-admin action outside this story's scope).
2. The CI workflow name/job names are stable and documented so they can be referenced when configuring branch protection.

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled — manual review only.

## Tasks / Subtasks
- [ ] Task 1: Update `README.md`'s CI section with the final job names (`scan_ruby`, `scan_js`, `lint`, `test`) and a note on required-status-check setup (AC: 1, 2)

## Dev Notes
- Depends on Story 6.1 (final job list/names must be settled first).
- This story is documentation-only — no code changes to `ci.yml` itself beyond what 6.1 already did.
- Actual GitHub branch protection rule configuration requires repo admin access and `gh`/web UI — out of scope for this story per `docs/prd/epic-6-ci-cd-security-gate.md`; `@devops` performs it once a remote exists (see Story 1.7 QA Results: no `gh` CLI/remote configured in this environment as of Epic 1).

### Testing
- No automated test — documentation verification only.

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-18 | 0.1 | Story criada a partir do Epic 6. | @sm (River) |
| 2026-06-18 | 0.2 | GO — Draft → Ready. | @po (Pax) |

## Dev Agent Record
### Agent Model Used
_(implementação pendente — próxima sessão)_

### Completion Notes List
(preenchido durante implementação)

### File List
(preenchido durante implementação)

## QA Results
(preenchido após implementação)
