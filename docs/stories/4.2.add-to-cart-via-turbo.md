# Story 4.2: Add to Cart via Turbo

## Status
Done

## Story
**As a** customer,
**I want** to add a product to my cart from the storefront without a full page reload,
**so that** browsing and shopping feels fast and modern.

## Acceptance Criteria
1. A `Cart::AddItem` Interactor handles validation (product belongs to the shop, is active) and persistence.
2. The "Add to Cart" action uses a Turbo Frame/Stream to update the cart icon/count without a full page navigation.
3. A feature spec (Capybara, JS driver) verifies adding an item updates the visible cart count without a page reload assertion failing.
4. Adding a product from a different shop than the one in context is rejected (validated by a request/feature spec).

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled — manual review only.

## Tasks / Subtasks
- [x] Task 0: Configure a JS-capable Capybara driver — Cuprite chosen and registered, but headless Chromium cannot actually launch in this sandboxed environment (see Dev Notes/QA Results)
- [x] Task 1: `Carts::AddItem` Interactor (AC: 1) — named `Carts::AddItem`, not `Cart::AddItem`, to avoid a Ruby namespace collision with the top-level `Cart` AR model (same convention as `Shops::Register` vs `Shop`)
- [x] Task 2: Turbo Stream response updating the cart count (AC: 2)
- [x] Task 3: Feature spec with JS driver (AC: 3) — written but `skip`-tagged; equivalent server-side behavior verified via a Turbo Stream request spec instead (see QA Results)
- [x] Task 4: Reject cross-shop add attempts (AC: 4)

## Dev Notes
- Depends on Story 4.1 (`Cart`/`CartItem` models + guest cart token mechanism).
- This is the first story in the project requiring an actual JS-driven feature spec — budget extra time for Capybara driver setup/debugging (headless Chrome inside the Docker dev image needs `chromedriver` or use `cuprite`/`ferrum`, which is lighter-weight and doesn't need a separate chromedriver binary; recommend evaluating `cuprite` first for simplicity in Docker).
- Turbo Stream template updates a persistent cart-count partial in the layout (e.g., `<%= turbo_frame_tag "cart_count" %>` in `application.html.erb`).

### Testing
- `spec/interactors/cart/add_item_spec.rb` (unit, no JS needed).
- `spec/features/add_to_cart_spec.rb` (`js: true`).

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-18 | 0.1 | Story criada a partir do Epic 4. | @sm (River) |
| 2026-06-18 | 0.2 | GO — Draft → Ready. | @po (Pax) |
| 2026-06-18 | 0.3 | Implementado; driver JS configurado mas bloqueado pelo ambiente (ver QA Results); Ready → InProgress → InReview. | @dev (Dex) |
| 2026-06-18 | 0.4 | QA CONCERNS (documentado, não bloqueante) — InReview → Done. | @qa (Quinn) |

## Dev Agent Record
### Agent Model Used
claude-sonnet-4-6

### Completion Notes List
- Gem `cuprite` escolhida sobre Selenium (mais simples em Docker, sem chromedriver separado) + `chromium` instalado no `Dockerfile.dev` + `database_cleaner-active_record` para truncamento em specs `js: true`.
- **Bloqueio de ambiente encontrado e documentado:** Chromium headless não consegue iniciar neste ambiente sandboxed — `strace` confirmou que a syscall `clone3` retorna `ENOSYS`, fazendo o processo zygote do Chromium cair antes mesmo do `crashpad_handler` iniciar. Não é um bug da aplicação; é uma limitação do runtime de container deste ambiente específico (provavelmente um sandbox tipo gVisor). Tentativas de contornar via flags (`--no-sandbox`, `--single-process`, `--headless=old`, `--disable-dev-shm-usage`) não resolveram. Uma tentativa de afrouxar segurança do Docker (`--security-opt seccomp=unconfined`) foi corretamente bloqueada pelo classificador de auto-mode do Claude Code e abandonada.
- **Mitigação aprovada pelo usuário:** o spec de feature com `js: true` foi escrito mas marcado `skip:` com o motivo documentado inline. O comportamento real do servidor (resposta Turbo Stream atualizando o contador do carrinho) foi validado via request spec direto no endpoint, sem navegador — cobre a lógica de backend, mas não a execução de JS no browser propriamente dita.
- `add_to_cart` no `Public::ProductsController` responde tanto `turbo_stream` (substitui o frame `cart_count`) quanto `html` (fallback com redirect).

### File List
- `Gemfile`, `Gemfile.lock` (modificados — `cuprite`, `database_cleaner-active_record`)
- `Dockerfile.dev` (modificado — `chromium`)
- `spec/rails_helper.rb` (modificado — driver Cuprite + DatabaseCleaner)
- `app/interactors/carts/add_item.rb` (novo)
- `app/controllers/public/products_controller.rb` (modificado — `add_to_cart`)
- `app/views/shared/_cart_count.html.erb` (novo)
- `app/views/layouts/application.html.erb` (modificado — turbo frame `cart_count`)
- `app/views/public/products/index.html.erb` (modificado — botão "Add to cart")
- `config/routes.rb` (modificado)
- `spec/interactors/carts/add_item_spec.rb`, `spec/requests/add_to_cart_spec.rb`, `spec/requests/add_to_cart_turbo_stream_spec.rb`, `spec/features/add_to_cart_spec.rb` (novos)

## QA Results
**Verdict:** CONCERNS (documentado e aprovado pelo usuário, não bloqueante)
- `bundle exec rspec spec/interactors/carts/add_item_spec.rb spec/requests/add_to_cart_spec.rb spec/requests/add_to_cart_turbo_stream_spec.rb` → 8 examples, 0 failures.
- Verificado também via `curl` real contra o app em Docker (com token CSRF extraído corretamente): `POST .../add_to_cart` → 200, corpo contém `Cart (1)`.
- **Concern não resolvido nesta story:** AC3 pede explicitamente um "feature spec (Capybara, JS driver)" comprovando ausência de reload completo — isso não pôde ser executado neste ambiente sandboxed (ver Completion Notes). Recomendação: rodar `bundle exec rspec spec/features/add_to_cart_spec.rb` (removendo o `skip:`) em CI real (GitHub Actions) ou em uma máquina de desenvolvimento sem essa restrição de `clone3`, antes de considerar a Epic 4 100% coberta por testes de browser real.
