# Story 3.2: Seller Product CRUD

## Status
Done

## Story
**As a** seller,
**I want** to create, edit, list, and deactivate products in my shop's admin area,
**so that** I can manage what I'm selling.

## Acceptance Criteria
1. `/:shop_slug/admin/products` supports index, new/create, edit/update, and a deactivate action (soft delete via `active: false`, not a hard destroy).
2. All actions are authorized via `Ability` (`can :manage, Product, shop_id: user.shop_id`).
3. Attempting to edit another shop's product (by guessing an ID) returns 404/403, verified by a request spec.
4. A feature spec covers creating a product through the form and seeing it appear in the index.

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled — manual review only.

## Tasks / Subtasks
- [x] Task 1: `Admin::ProductsController` (index/new/create/edit/update/deactivate) (AC: 1)
- [x] Task 2: `Ability` rule for `Product` (AC: 2)
- [x] Task 3: Request spec for cross-tenant denial (AC: 3)
- [x] Task 4: Feature spec for create → appears in index (AC: 4)

## Dev Notes
- Depends on Story 3.1 (`Product` model) and Epic 2 (`Admin` namespace, tenant resolution, `Ability` pattern already established for `Shop`).
- Deactivate = `PATCH` setting `active: false`, not `DELETE` — no destroy action exposed, per AC1.
- Cross-tenant access: because `Product` is `acts_as_tenant(:shop)`-scoped, `Product.find(id)` from a different shop's controller context already raises `ActiveRecord::RecordNotFound` (404) automatically — CanCan `authorize!` is a second layer of defense (consistent with the dual-layer pattern from `docs/architecture.md`: acts_as_tenant for data isolation, CanCan for action authorization).

### Testing
- `spec/requests/admin/products_spec.rb`, `spec/features/seller_product_management_spec.rb`.

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-18 | 0.1 | Story criada a partir do Epic 3. | @sm (River) |
| 2026-06-18 | 0.2 | GO — Draft → Ready. | @po (Pax) |
| 2026-06-18 | 0.3 | Implementado e verificado; Ready → InProgress → InReview. | @dev (Dex) |
| 2026-06-18 | 0.4 | QA PASS — InReview → Done. | @qa (Quinn) |

## Dev Agent Record
### Agent Model Used
claude-sonnet-4-6

### Completion Notes List
- Rotas restritas a `only: %i[index new create edit update]` + `deactivate` customizado — sem `show`/`destroy` expostos no admin, conforme AC1 (soft delete apenas).
- Cross-tenant denial (AC3) vem "de graça" via `acts_as_tenant`: `Product.find(id)` de outra loja já levanta `ActiveRecord::RecordNotFound` (404) antes mesmo do `authorize!` rodar, porque o default_scope do tenant já filtra a query. `authorize!` é a segunda camada (defesa em profundidade), redundante neste caso específico mas necessária para outras ações.

### File List
- `app/controllers/admin/products_controller.rb` (novo)
- `app/views/admin/products/index.html.erb`, `new.html.erb`, `edit.html.erb`, `_form.html.erb` (novos)
- `app/views/admin/dashboard/show.html.erb` (modificado — link para produtos)
- `app/models/ability.rb` (modificado — regra para `Product`)
- `config/routes.rb` (modificado)
- `spec/requests/admin/products_spec.rb`, `spec/features/seller_product_management_spec.rb` (novos)

## QA Results
**Verdict:** PASS
- `bundle exec rspec spec/requests/admin/products_spec.rb spec/features/seller_product_management_spec.rb` → verde (404 cross-tenant, criação aparece no index).
- Verificado também via app real: dashboard em Docker exibe o link `/curl-test-shop/admin/products`.
