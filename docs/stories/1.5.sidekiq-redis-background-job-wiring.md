# Story 1.5: Sidekiq and Redis Background Job Wiring

## Status
Done

## Story
**As a** developer,
**I want** Sidekiq configured as the Active Job adapter, connected to the Redis service from docker-compose,
**so that** asynchronous jobs have a working execution path from day one.

## Acceptance Criteria
1. `config.active_job.queue_adapter = :sidekiq` is set.
2. `config/sidekiq.yml` and Redis connection config read the Redis URL from an environment variable matching the docker-compose `redis` service.
3. A `worker` service in `docker-compose.yml` runs `bundle exec sidekiq` against the same codebase as `web`.
4. A trivial smoke-test job (`HealthCheckJob`) can be enqueued and executes successfully, verified via a job spec using `ActiveJob::TestHelper`.

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled — manual review only.

## Tasks / Subtasks
- [x] Task 1: Set Active Job adapter to `:sidekiq` (AC: 1) — set globally in `config/application.rb`, overridden to `:test` in `config/environments/test.rb` so `ActiveJob::TestHelper` works; also replaced the Rails 8 default `:solid_queue` adapter previously set in `config/environments/production.rb`
- [x] Task 2: Configure `config/sidekiq.yml` + Redis URL from ENV (AC: 2)
- [x] Task 3: `worker` service already existed in `docker-compose.yml` (added ahead of schedule in Story 1.1) (AC: 3)
- [x] Task 4: Create `HealthCheckJob` + job spec (AC: 4)

## Dev Notes
- Depends on Story 1.1 (`docker-compose.yml` already has `redis` service from that story; this story adds `worker`).
- Tech stack pin: Redis 7, Sidekiq ~> 7.x (`docs/framework/tech-stack.md`).

### Testing
- `spec/jobs/health_check_job_spec.rb` using `ActiveJob::TestHelper`'s `perform_enqueued_jobs`.

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-17 | 0.1 | Story criada a partir do Epic 1. | @sm (River) |
| 2026-06-17 | 0.2 | GO — Draft → Ready. | @po (Pax) |
| 2026-06-18 | 0.3 | Implementado e verificado fim-a-fim (job real via Redis dockerizado); Ready → InProgress → InReview. | @dev (Dex) |
| 2026-06-18 | 0.4 | QA PASS — InReview → Done. | @qa (Quinn) |

## Dev Agent Record
### Agent Model Used
claude-sonnet-4-6

### Completion Notes List
- Decisão de design: `queue_adapter = :sidekiq` definido em `config/application.rb` (aplica a todos os ambientes), com override para `:test` em `config/environments/test.rb` — necessário para os matchers de `ActiveJob::TestHelper` (`have_enqueued_job`) funcionarem sem depender de Redis real durante a suíte.
- Removida a configuração `:solid_queue` que o scaffold padrão do Rails 8 havia colocado em `config/environments/production.rb`, substituída por Sidekiq+Redis em todos os ambientes, conforme mandato do usuário.

### File List
- `config/application.rb` (modificado)
- `config/environments/production.rb` (modificado — removido `solid_queue`)
- `config/environments/test.rb` (modificado — `queue_adapter = :test`)
- `config/sidekiq.yml`, `config/initializers/sidekiq.rb` (novos)
- `app/jobs/health_check_job.rb`, `spec/jobs/health_check_job_spec.rb` (novos)

## QA Results
**Verdict:** PASS
- `bundle exec rspec spec/jobs/health_check_job_spec.rb` → 2 examples, 0 failures.
- Verificação real fim-a-fim: `HealthCheckJob.perform_later` enfileirado pelo `web` foi consumido e executado pelo `worker` via Redis dockerizado (confirmado nos logs do Sidekiq: `start` → `Performing` → `executed at` → `done`).
