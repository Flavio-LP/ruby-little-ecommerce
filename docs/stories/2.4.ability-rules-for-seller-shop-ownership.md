# Story 2.4: Ability Rules for Seller/Shop Ownership

## Status
Done

## Story
**As a** developer,
**I want** `Ability` rules that scope seller permissions to their own `Shop`,
**so that** authorization and tenant isolation work together consistently across the app.

## Acceptance Criteria
1. `Ability` grants sellers `:manage` on resources where `shop_id == user.shop_id` (pattern to be reused by `Product`/`Order` in later epics).
2. `Ability` denies sellers any action on resources belonging to a different `shop_id`.
3. A model/unit spec for `Ability` verifies both the grant and the denial case using a stub/double resource.

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled — manual review only.

## Tasks / Subtasks
- [x] Task 1: Implement `can :manage, Shop, id: user.shop_id` (and a generic pattern note for future models) (AC: 1)
- [x] Task 2: Verify denial for a different shop_id (AC: 2)
- [x] Task 3: Unit spec (AC: 3)

## Dev Notes
- Builds on the placeholder `Ability` from Story 1.4.
- This story's rule applies concretely to `Shop` (the only seller-owned resource that exists at this point in the epic sequence); `Product`/`Order` rules following the same `shop_id == user.shop_id` pattern are added in Epic 3/5 when those models exist — do not pre-create rules for models that don't exist yet (`docs/framework/coding-standards.md`: no invented code ahead of need).

### Testing
- `spec/models/ability_spec.rb` using a double/instance_double for a stand-in resource with `shop_id`.

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-18 | 0.1 | Story criada a partir do Epic 2. | @sm (River) |
| 2026-06-18 | 0.2 | GO — Draft → Ready. | @po (Pax) |
| 2026-06-18 | 0.3 | Implementado e verificado; Ready → InProgress → InReview. | @dev (Dex) |
| 2026-06-18 | 0.4 | QA PASS — InReview → Done. | @qa (Quinn) |

## Dev Agent Record
### Agent Model Used
claude-sonnet-4-6

### Completion Notes List
- `can :manage, Shop, id: user.shop_id` adicionado ao bloco `if user.seller?` da `Ability` (já existente desde a Story 1.4).
- Usado `cancan/matchers` (`be_able_to`) nos specs — exigiu adicionar `require 'cancan/matchers'` em `spec/rails_helper.rb`.

### File List
- `app/models/ability.rb` (modificado)
- `spec/rails_helper.rb` (modificado — `require 'cancan/matchers'`)
- `spec/models/ability_spec.rb` (novo)

## QA Results
**Verdict:** PASS
- `bundle exec rspec spec/models/ability_spec.rb` → 3 examples, 0 failures (concede para a própria loja, nega para loja diferente, nega para usuário nil).
