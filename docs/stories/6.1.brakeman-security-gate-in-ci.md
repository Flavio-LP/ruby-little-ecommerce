# Story 6.1: Brakeman Security Gate in CI

## Status
Done

## Story
**As a** developer,
**I want** the CI pipeline to run Brakeman and fail the build on high/critical findings,
**so that** security regressions are caught before merge, not after deploy.

## Acceptance Criteria
1. `.github/workflows/ci.yml` adds a `bundle exec brakeman -A -q --exit-on-warn --confidence-level=2` (or equivalent threshold) step.
2. The job fails the workflow when Brakeman finds confidence-level findings at or above the configured threshold.
3. Any pre-existing findings as of this story are either fixed or explicitly added to a documented Brakeman ignore file (`config/brakeman.ignore`) with a one-line justification per ignored finding — not silently suppressed.
4. The workflow is verified to actually fail on a deliberately introduced vulnerable code snippet in a throwaway test branch, then verified green after removing it.

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled — manual review only.

## Tasks / Subtasks
- [x] Task 1: Re-add the `scan_ruby` job to `.github/workflows/ci.yml` (removed in Story 1.7 pending this story) (AC: 1, 2)
- [x] Task 2: Triage current findings — re-ran Brakeman against the full Epic 1-5 codebase; still only 1 finding (`EOLRuby`). Added a justified entry to `config/brakeman.ignore` rather than upgrading Ruby now (AC: 3)
- [x] Task 3: Verify red→green — done locally (no `gh`/remote available to push a throwaway branch, see Story 1.7); introduced a deliberate SQL injection snippet, confirmed Brakeman exits 3 (fail), removed it, confirmed exit 0 (AC: 4, adapted)

## Dev Notes
- **Important context carried over from Story 1.7's QA Results:** the `scan_ruby`/Brakeman job already existed in the Rails 8 scaffold's `ci.yml` but was deliberately removed in Story 1.7 because it would fail untriaged (the `EOLRuby` finding) — this story is exactly where that gets resolved properly, not a fresh addition.
- Re-run `bin/brakeman --no-pager` first thing in this story to get a current findings list — more app code has been added since Story 1.7 (Epics 2-5), so don't assume only the one known finding still applies.
- `config/brakeman.ignore` is generated via `bin/brakeman -I` (interactive) or hand-written; each entry needs a `note` field with justification per `docs/prd/epic-6-ci-cd-security-gate.md`.

### Testing
- No RSpec test — verification is the CI run itself (push to a throwaway branch with an intentionally vulnerable snippet, confirm red, remove it, confirm green).

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-18 | 0.1 | Story criada a partir de `docs/prd/epic-6-ci-cd-security-gate.md`. | @sm (River) |
| 2026-06-18 | 0.2 | GO — Draft → Ready. | @po (Pax) |
| 2026-06-18 | 0.3 | Implementado e verificado (incluindo simulação local de red→green); Ready → InProgress → InReview. | @dev (Dex) |
| 2026-06-18 | 0.4 | QA PASS (com nota) — InReview → Done. | @qa (Quinn) |

## Dev Agent Record
### Agent Model Used
claude-sonnet-4-6

### Completion Notes List
- `config/brakeman.ignore` criado com o fingerprint exato do finding `EOLRuby` (obtido via `bin/brakeman -f json`) e uma nota explicando por que não foi corrigido agora (upgrade de Ruby é mudança de infra separada e deliberada) e quando revisitar.
- `ci.yml`: `scan_ruby` usa `bin/brakeman -i config/brakeman.ignore --confidence-level=2 --no-pager`.
- AC4 ("verificar falha/sucesso em uma branch de teste real") foi adaptada: como não há `gh`/remote configurado para abrir uma branch real no GitHub (limitação já registrada na Story 1.7), a verificação red→green foi feita localmente dentro do container — criei um controller com SQL injection deliberada, confirmei `bin/brakeman` retornando exit code 3 (falha), removi o controller, confirmei exit code 0 (sucesso).

### File List
- `config/brakeman.ignore` (novo)
- `.github/workflows/ci.yml` (modificado — job `scan_ruby` reintroduzido)

## QA Results
**Verdict:** PASS (com nota)
- `bin/brakeman -i config/brakeman.ignore --confidence-level=2` → exit 0, 0 warnings, 1 ignorado e justificado.
- Ciclo red→green confirmado localmente (SQL injection introduzida → exit 3; removida → exit 0).
- **Nota:** a verificação "em uma branch/PR real do GitHub Actions" pedida literalmente pela AC4 não pôde ocorrer (sem `gh auth`/remote configurado neste ambiente até a Story 1.7). A simulação local é equivalente em conteúdo (mesmo comando, mesmo ambiente Docker), mas não comprova a integração real com o GitHub Actions runner — isso deve ser conferido por `@devops` no primeiro push real.
